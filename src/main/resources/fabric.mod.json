{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${name}",
  "description": "${description}",
  "authors": [
    "OffsetMonkey538"
  ],
  "contact": {
    "sources": "${sources}"
  },

  "license": "${license}",
  "icon": "assets/${id}/icon.png",
  "environment": "*",
  "entrypoints": {

  },
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {
    "minecraft": "~1.20"
  },
  "custom": {
    "modmanager": {
      "modrinth": "${modrinthProject}"
    }
  }
}
